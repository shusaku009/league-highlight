<% if @comment.valid? %>
  <%= turbo_stream.replace(@comment) %>
<% else %>
  <%= turbo_stream.replace(
        dom_id(@comment, 'form'),
        partial: 'form',
        locals: { video: @comment.video, comment: @comment }
      ) %>
<% end %>
