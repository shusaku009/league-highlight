<div class="flex-1 overflow-x-auto">
  <div class="bg-gray-800">
    <div class="flex justify-between">
      <%# 動画を新規登録する機能を実装するまでは一旦保留 %>
      <%= link_to '新規登録', "#", class: 'm-5 btn btn-success' %>
      <%# ここまで %>
      <%= link_to 'コメント一覧', admin_comments_path, class: 'm-5 btn' %>
    </div>
    <div class="pb-4">
      <%= render 'admin/shared/comment_search', q: @q, url: admin_comments_path %>
    </div>
  </div>
  <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
    <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
      <tr>
        <th scope="col" class="px-6 py-3">
          コメント
        </th>
        <th scope="col" class="px-6 py-3">
          削除
        </th>
      </tr>
    </thead>
    <tbody>
      <% @comments.each do |comment| %>
        <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
          <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
            <%= comment.body %>
          </th>
          <td class="px-6 py-4">
            <%= link_to '削除', admin_comment_path(comment), data: { turbo_method: :delete, turbo_confirm: "ユーザー「#{comment.body}」を削除します。よろしいですか？" }, class: 'btn btn-error' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <div class="flex justify-center my-6">
    <%== pagy_nav(@pagy) %>
  </div>
</div>
